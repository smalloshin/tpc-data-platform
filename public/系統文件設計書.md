# 重要營運詮釋資料服務平台 - 系統文件設計書

**版本**: 1.0  
**建立日期**: 2025-12-11  
**系統名稱**: 重要營運詮釋資料服務平台  

---

## 目錄

1. [系統概述](#1-系統概述)
2. [系統架構](#2-系統架構)
3. [技術堆疊](#3-技術堆疊)
4. [元件結構](#4-元件結構)
5. [資料結構設計 (Data Schema)](#5-資料結構設計-data-schema)
6. [功能模組說明](#6-功能模組說明)
7. [資料流程](#7-資料流程)
8. [檔案目錄結構](#8-檔案目錄結構)

---

## 1. 系統概述

### 1.1 系統目的
本系統為「重要營運詮釋資料服務平台」，旨在提供基於知識圖譜的智能搜尋引擎，協助使用者快速查找與電力產業相關的資料集。系統涵蓋發電、輸電、配電、售電及其他類別的資料集搜尋與瀏覽功能。

### 1.2 主要功能
- **類別導覽**: 提供發電、輸電、配電、售電、探索五大類別入口
- **關鍵字搜尋**: 支援關鍵字輸入與自動補全建議
- **情境導引搜尋**: 透過預設情境快速找到相關資料集
- **知識圖譜視覺化**: 以 D3.js 呈現概念、關鍵字、資料集之間的關聯
- **FAQ 問答**: 提供常見問題與推薦資料集
- **資料集瀏覽器**: 列表式瀏覽所有資料集並支援排序篩選

### 1.3 目標使用者
- 電力產業研究人員
- 資料分析師
- 政策制定者
- 一般大眾

---

## 2. 系統架構

### 2.1 架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端應用層 (Frontend)                      │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                     React + TypeScript                       │ │
│  │  ┌──────────────┬──────────────┬──────────────────────────┐ │ │
│  │  │ CategoryLanding│SearchInterface│   DatasetBrowser       │ │ │
│  │  └──────────────┴──────────────┴──────────────────────────┘ │ │
│  │  ┌──────────────┬──────────────┬──────────────────────────┐ │ │
│  │  │KnowledgeGraphD3│ FAQSection  │   ConceptExplorer        │ │ │
│  │  └──────────────┴──────────────┴──────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        資料層 (Data Layer)                       │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    靜態資料檔案 (public/data/)                 │ │
│  │  ┌──────────────┬──────────────┬──────────────────────────┐ │ │
│  │  │   *.xlsx     │    *.json    │     Knowledge Graph      │ │ │
│  │  │ (Excel資料)   │  (FAQ/情境)   │   (圖譜結構資料)          │ │ │
│  │  └──────────────┴──────────────┴──────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 部署架構

```
┌─────────────────┐     ┌─────────────────┐
│   使用者瀏覽器    │────▶│   Lovable CDN   │
│   (Client)      │◀────│   (Hosting)     │
└─────────────────┘     └─────────────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │   靜態資源服務    │
                        │ (HTML/JS/CSS/   │
                        │  JSON/XLSX)     │
                        └─────────────────┘
```

---

## 3. 技術堆疊

### 3.1 前端框架
| 技術 | 版本 | 用途 |
|------|------|------|
| React | ^18.3.1 | UI 框架 |
| TypeScript | - | 型別安全 |
| Vite | - | 建構工具 |
| React Router DOM | ^6.30.1 | 路由管理 |

### 3.2 UI 元件庫
| 技術 | 版本 | 用途 |
|------|------|------|
| Tailwind CSS | - | CSS 框架 |
| shadcn/ui | - | UI 元件庫 |
| Radix UI | ^1.x | 無障礙元件 |
| Lucide React | ^0.462.0 | 圖示庫 |

### 3.3 資料視覺化
| 技術 | 版本 | 用途 |
|------|------|------|
| D3.js | ^7.9.0 | 知識圖譜視覺化 |
| Recharts | ^2.15.4 | 圖表元件 |

### 3.4 資料處理
| 技術 | 版本 | 用途 |
|------|------|------|
| xlsx | ^0.18.5 | Excel 檔案解析 |

---

## 4. 元件結構

### 4.1 元件階層圖

```
App.tsx
└── Index.tsx (主頁面)
    ├── CategoryLanding.tsx (類別選擇)
    ├── SearchInterface.tsx (搜尋介面)
    │   ├── FAQSection.tsx (FAQ 區塊)
    │   ├── ConceptExplorer.tsx (概念探索)
    │   ├── KnowledgeGraphD3.tsx (知識圖譜)
    │   ├── ResponsibleUnitExplorer.tsx (責任單位探索)
    │   ├── OtherSituationExplorer.tsx (其他情境探索)
    │   ├── DatasetDetailDialog.tsx (資料集詳情彈窗)
    │   └── OtherSystemDetailDialog.tsx (其他系統詳情彈窗)
    └── DatasetBrowser.tsx (資料集瀏覽器)
        └── DatasetDetailDialog.tsx (資料集詳情彈窗)
```

### 4.2 核心元件說明

| 元件名稱 | 檔案路徑 | 功能說明 |
|----------|----------|----------|
| CategoryLanding | src/components/CategoryLanding.tsx | 首頁類別卡片選擇介面 |
| SearchInterface | src/components/SearchInterface.tsx | 類別搜尋主介面 |
| DatasetBrowser | src/components/DatasetBrowser.tsx | 全資料集列表瀏覽 |
| KnowledgeGraphD3 | src/components/KnowledgeGraphD3.tsx | D3 力導向知識圖譜 |
| FAQSection | src/components/FAQSection.tsx | 常見問題摺疊面板 |
| ConceptExplorer | src/components/ConceptExplorer.tsx | 概念分類探索器 |
| DatasetDetailDialog | src/components/DatasetDetailDialog.tsx | 資料集詳情/範例彈窗 |

---

## 5. 資料結構設計 (Data Schema)

### 5.1 資料集詳細資訊 (DatasetDetail)

**來源檔案**: `public/data/dataset_details.xlsx`

```typescript
interface DatasetDetail {
  department: string;    // 部門名稱
  id: string;            // 資料集 ID
  name: string;          // 資料集名稱
  description: string;   // 資料集詳細說明
  sampleData: string;    // 範例資料 (JSON 字串)
  summary: string;       // 資料集總結 (AI 生成)
}
```

**Excel 欄位對應**:
| Excel 欄位 | Schema 屬性 | 說明 |
|------------|-------------|------|
| 部門 | department | 資料集所屬部門 |
| 資料集ID | id | 唯一識別碼 |
| 資料集名稱 | name | 資料集顯示名稱 |
| 資料集詳細說明 | description | 詳細描述文字 |
| 範例資料 | sampleData | JSON 格式範例 |

### 5.2 資料集總結 (DatasetSummary)

**來源檔案**: `public/data/dataset_summary.xlsx`

| Excel 欄位 | 說明 |
|------------|------|
| 資料集名稱 | 對應資料集名稱 |
| 資料集總結 | AI 生成的摘要說明 |

### 5.3 知識圖譜結構 (KnowledgeGraph)

**來源檔案**: `public/data/{category}_knowledge_graph.json`

```typescript
interface KnowledgeGraphData {
  nodes: GraphNode[];
  links: GraphLink[];  // 或 edges
}

interface GraphNode {
  id: string;                              // 節點唯一識別碼
  label: string;                           // 顯示標籤
  type: 'concept' | 'keyword' | 'dataset'; // 節點類型
  category: string;                        // 所屬類別
  stage?: string;                          // 匹配階段
  color: string;                           // 顯示顏色
  size: number;                            // 節點大小
}

interface GraphLink {
  source: string;  // 來源節點 ID
  target: string;  // 目標節點 ID
  type: string;    // 連結類型 (如 'belongs_to', 'related_to')
  stage?: string;  // 匹配階段
}
```

**節點類型說明**:
| 類型 | 說明 | 顏色建議 |
|------|------|----------|
| concept | 概念節點 | 藍色系 |
| keyword | 關鍵字節點 | 綠色系 |
| dataset | 資料集節點 | 橘色系 |

### 5.4 匹配結果 (MatchingResults)

**來源檔案**: `public/data/{category}_matching_results.json`

```typescript
interface MatchingResultsData {
  matching_results: MatchingRecord[];
}

interface MatchingRecord {
  關鍵字: string;      // 搜尋關鍵字
  資料集名稱: string;   // 匹配到的資料集
  匹配階段: string;    // 第一階段/第二階段/第三階段
  匹配方式: string;    // 匹配方法描述
  匹配原因: string;    // 匹配原因說明
  相關性分數: number;  // 0-10 分
}
```

### 5.5 情境資料 (Situations)

**來源檔案**: `public/data/{category}_situations.json`

```typescript
interface SituationsData {
  situations: Situation[];
}

interface Situation {
  name: string;        // 情境名稱
  icon: string;        // 圖示 (emoji)
  description: string; // 情境描述
  concepts: string[];  // 相關概念列表
}
```

### 5.6 FAQ 資料

**來源檔案**: `public/data/{category}_faq.json`

```typescript
interface FAQData {
  faqs: FAQ[];
}

interface FAQ {
  question: string;               // 問題
  answer: string;                 // 回答
  datasets: DatasetReference[];   // 推薦資料集
}

interface DatasetReference {
  id: string;     // 資料集 ID
  title: string;  // 資料集標題
  url?: string;   // 資料集連結 (選填)
}
```

### 5.7 其他系統資料 (OtherSystems)

**來源檔案**: `public/data/other_systems.xlsx`

```typescript
interface SystemData {
  systemName: string;        // 系統名稱
  managementTarget: string;  // 管理標的
  themeL1: string;           // 主題領域 L1
  themeL2: string;           // 主題領域 L2
  themeL3: string;           // 主題領域 L3
  dataDescription: string;   // 資料說明
  dataFormat: string;        // 資料格式
  updateFrequency: string;   // 更新頻率
  responsibleUnit: string;   // 負責單位
}
```

---

## 6. 功能模組說明

### 6.1 類別導覽模組

**功能**: 提供五大類別的視覺化入口

| 類別 ID | 類別名稱 | 圖示 | 說明 |
|---------|----------|------|------|
| generation | 發電類別 | 🔋 | 發電設施、發電量、能源結構相關資料 |
| transmission | 輸電類別 | ⚡ | 輸配電設施、電力調度、系統運作相關資料 |
| distribution | 配電類別 | 🔌 | 配電系統、用戶服務、供電品質相關資料 |
| retail | 售電類別 | 💡 | 電力銷售、電價、用電統計相關資料 |
| other | 探索 | 📁 | 其他相關資料 |

### 6.2 搜尋模組

**搜尋方式**:
1. **關鍵字搜尋**: 直接輸入關鍵字，系統根據匹配結果返回相關資料集
2. **情境導引**: 選擇預設情境，系統透過知識圖譜找到相關資料集
3. **概念探索**: 瀏覽概念分類，點擊概念查看關聯資料集
4. **FAQ 推薦**: 從常見問題中獲取推薦資料集

**相關性計算公式**:
```typescript
relevance = Σ(stageWeight × relevanceScore) / recordCount

stageWeight = {
  '第一階段': 1.0,
  '第二階段': 0.85,
  '第三階段': 0.7
}
```

### 6.3 知識圖譜模組

**功能特點**:
- D3.js 力導向圖視覺化
- 支援縮放與拖曳
- 節點點擊高亮關聯路徑
- 類型與階段篩選
- 關鍵字搜尋節點

### 6.4 資料集瀏覽模組

**功能**:
- 列表式顯示所有資料集
- 支援多欄位排序 (部門、ID、名稱)
- 關鍵字搜尋篩選
- 點擊查看詳細資訊/範例資料

---

## 7. 資料流程

### 7.1 搜尋流程

```
使用者輸入關鍵字
        │
        ▼
┌───────────────────┐
│ 載入 matching_results.json │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   篩選匹配記錄      │
│ (by 關鍵字欄位)     │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   計算相關性分數    │
│ (依階段加權平均)    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   排序並顯示結果    │
└───────────────────┘
```

### 7.2 資料載入流程

```
應用程式啟動
        │
        ▼
┌───────────────────┐
│ 載入 Excel 檔案    │
│ (xlsx library)    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ 解析並建立快取     │
│ (Map<string, DatasetDetail>) │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ 切換類別時載入     │
│ 對應 JSON 檔案    │
└───────────────────┘
```

---

## 8. 檔案目錄結構

```
project-root/
├── public/
│   ├── data/
│   │   ├── dataset_details.xlsx      # 資料集詳細資訊
│   │   ├── dataset_summary.xlsx      # 資料集總結
│   │   ├── other_systems.xlsx        # 其他系統資料
│   │   ├── generation_faq.json       # 發電類 FAQ
│   │   ├── generation_knowledge_graph.json
│   │   ├── generation_matching_results.json
│   │   ├── generation_situations.json
│   │   ├── transmission_faq.json     # 輸電類 FAQ
│   │   ├── transmission_knowledge_graph.json
│   │   ├── transmission_matching_results.json
│   │   ├── transmission_situations.json
│   │   ├── distribution_faq.json     # 配電類 FAQ
│   │   ├── distribution_knowledge_graph.json
│   │   ├── distribution_matching_results.json
│   │   ├── distribution_situations.json
│   │   ├── retail_faq.json           # 售電類 FAQ
│   │   ├── retail_knowledge_graph.json
│   │   ├── retail_matching_results.json
│   │   └── retail_situations.json
│   └── robots.txt
├── src/
│   ├── components/
│   │   ├── ui/                       # shadcn/ui 元件
│   │   ├── CategoryLanding.tsx
│   │   ├── SearchInterface.tsx
│   │   ├── DatasetBrowser.tsx
│   │   ├── DatasetDetailDialog.tsx
│   │   ├── KnowledgeGraphD3.tsx
│   │   ├── FAQSection.tsx
│   │   ├── ConceptExplorer.tsx
│   │   ├── ResponsibleUnitExplorer.tsx
│   │   ├── OtherSituationExplorer.tsx
│   │   ├── OtherSystemDetailDialog.tsx
│   │   └── NavLink.tsx
│   ├── hooks/
│   │   ├── use-mobile.tsx
│   │   └── use-toast.ts
│   ├── lib/
│   │   └── utils.ts
│   ├── pages/
│   │   ├── Index.tsx                 # 主頁面
│   │   └── NotFound.tsx
│   ├── utils/
│   │   ├── datasetLoader.ts          # 資料集載入工具
│   │   ├── otherSystemsLoader.ts     # 其他系統載入工具
│   │   └── excelMerger.ts            # Excel 合併工具
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── index.html
├── tailwind.config.ts
├── vite.config.ts
└── package.json
```

---

## 附錄 A: API 端點 (靜態資源)

本系統為純前端應用，所有資料透過靜態檔案提供：

| 資源路徑 | 說明 |
|----------|------|
| `/data/dataset_details.xlsx` | 資料集詳細資訊 |
| `/data/dataset_summary.xlsx` | 資料集總結 |
| `/data/{category}_knowledge_graph.json` | 知識圖譜資料 |
| `/data/{category}_matching_results.json` | 匹配結果資料 |
| `/data/{category}_situations.json` | 情境資料 |
| `/data/{category}_faq.json` | FAQ 資料 |
| `/data/other_systems.xlsx` | 其他系統資料 |

---

## 附錄 B: 環境需求

| 項目 | 需求 |
|------|------|
| Node.js | >= 18.x |
| 瀏覽器 | Chrome, Firefox, Safari, Edge (最新版本) |
| 螢幕解析度 | >= 1024 x 768 |

---

**文件結束**
